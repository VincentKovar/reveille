{
  "name": "reveille",
  "version": "2.0.0",
  "private": true,
  "description": "An alarm clock that wakes you with a poem read aloud.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "python3 -m http.server 8125 --directory www",
    "test": "node --test tests/*.test.mjs",
    "icons": "node scripts/make-icons.mjs",
    "sync": "npx cap sync android",
    "build:android": "node scripts/build-android.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
